from google.colab import files
uploaded = files.upload()

import numpy as np
import pandas as pd
from sklearn.linear_model import LinearRegression
from sklearn.metrics import mean_absolute_error, mean_squared_error, r2_score

# read dataset
data = pd.read_csv("salary_data.csv")

# independent and dependent variables
X = data[['Experience']]
y = data['Salary']

# build model
model = LinearRegression()
model.fit(X, y)

# prediction
y_pred = model.predict(X)

# evaluation metrics
sse = np.sum((y - y_pred) ** 2)
msse = sse / len(y)
mae = mean_absolute_error(y, y_pred)
rmse = np.sqrt(mean_squared_error(y, y_pred))
rmsle = np.sqrt(np.mean((np.log1p(y_pred) - np.log1p(y)) ** 2))
r2 = r2_score(y, y_pred)

print("SSE:", sse)
print("MSSE:", msse)
print("MAE:", mae)
print("RMSE:", rmse)
print("RMSLE:", rmsle)
print("R2 Score:", r2)



1. Problem Understanding & Aim (CO1)
Aim. To predict employee salary based on experience using Simple Linear Regression.

2. Data Collection & Pre-Processing (CO6)
Dataset contains two variables. Experience and Salary.
Data is loaded from CSV file using pandas.
Experience is taken as X and Salary as y.

3. Model Design / Algorithm Selection (CO2, CO3)
Simple Linear Regression is used.
It finds the relationship between experience and salary using a straight line.

4. Implementation – Model Building (CO3, CO4, CO5)
Import libraries.
Load dataset.
Separate X and y variables.
Train Linear Regression model.
Predict salary values.

5. Evaluation & Output (CO7)
Model performance evaluated using.
SSE, MSSE, MAE, RMSE, RMSLE and R² score.

6. Viva / Concept Clarity (CO1, CO4)
Simple Linear Regression predicts continuous values using one independent variable and one dependent variable.
